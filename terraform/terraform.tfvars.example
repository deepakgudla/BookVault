aws_region    = "ap-south-2"
bucket_name   = "replace-with-a-globally-unique-bucket-name"
object_prefix = "products/"
queue_name    = "bookvault-events"

# Optional: attach both generated policies to an existing task/instance role.
# iam_role_name = "replace-with-your-existing-iam-role"